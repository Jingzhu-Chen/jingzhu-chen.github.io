source "https://rubygems.org"

gem "jekyll", "~> 3.8.5"

gem "webrick", "~> 1.8"
gem "csv"     # Ruby 3.4+ 不再默认包含，Jekyll 需要
gem "base64"     # Ruby 3.4+ 不再默认包含，safe_yaml 需要
gem "bigdecimal" # Ruby 3.4+ 不再默认包含，liquid 需要
gem "rexml"       # Ruby 3.4+ 不再默认包含，kramdown 需要
